<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.mapper.LoggerMapper">
    <insert id="addLogger">
        insert into logger(user_id, method, param, execution_time,result)
        values(#{logger.userId}, #{logger.method}, #{logger.param}, #{logger.executionTime}, #{logger.result})
    </insert>
</mapper>
